<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Remove Petani Belum Tebus - PIMON</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #2c5aa0;
            --secondary-color: #6c757d;
            --success-color: #198754;
            --warning-color: #ffc107;
            --danger-color: #dc3545;
            --light-bg: #f8f9fa;
            --gradient-primary: linear-gradient(135deg, #66c7ea 0%, #4ba28c 100%);
            --gradient-secondary: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            --shadow: 0 4px 6px rgba(0,0,0,0.1);
            --shadow-hover: 0 10px 15px rgba(0,0,0,0.1);
        }
        
        body {
            background-color: #f5f7fb;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .card {
            box-shadow: var(--shadow);
            border: none;
            border-radius: 15px;
            transition: transform 0.3s, box-shadow 0.3s;
        }
        
        .card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-hover);
        }
        
        .card-header {
            border-radius: 15px 15px 0 0 !important;
            background: var(--gradient-primary);
        }
        
        .btn-primary {
            background: var(--gradient-primary);
            border: none;
            border-radius: 8px;
            padding: 10px 20px;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }
        
        .btn-success {
            background: var(--gradient-secondary);
            border: none;
            border-radius: 8px;
            padding: 10px 20px;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .btn-success:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(240, 147, 251, 0.4);
        }
        
        .status-success { color: var(--success-color); }
        .status-warning { color: var(--warning-color); }
        .status-danger { color: var(--danger-color); }
        
        .upload-area {
            border: 2px dashed #dee2e6;
            border-radius: 10px;
            padding: 2rem;
            text-align: center;
            transition: all 0.3s;
            background-color: white;
        }
        
        .upload-area:hover {
            border-color: var(--primary-color);
            background-color: var(--light-bg);
        }
        
        .upload-area.dragover {
            border-color: var(--primary-color);
            background-color: #e9ecef;
            transform: scale(1.02);
        }
        
        .stats-card {
            background: var(--gradient-primary);
            color: white;
            border-radius: 10px;
            padding: 1.5rem;
            margin-bottom: 1rem;
        }
        
        .condition-info {
            background-color: var(--light-bg);
            border-left: 4px solid var(--primary-color);
            padding: 1rem;
            margin: 1rem 0;
            border-radius: 0 5px 5px 0;
        }
        
        .step-indicator {
            display: flex;
            justify-content: space-between;
            margin-bottom: 2rem;
            position: relative;
        }
        
        .step-indicator::before {
            content: '';
            position: absolute;
            top: 20px;
            left: 0;
            right: 0;
            height: 2px;
            background-color: #e9ecef;
            z-index: 1;
        }
        
        .step {
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
            z-index: 2;
            flex: 1;
        }
        
        .step-number {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: #e9ecef;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            margin-bottom: 0.5rem;
            transition: all 0.3s;
        }
        
        .step.active .step-number {
            background: var(--gradient-primary);
            color: white;
        }
        
        .step.completed .step-number {
            background: var(--success-color);
            color: white;
        }
        
        .step-label {
            font-size: 0.9rem;
            font-weight: 500;
            text-align: center;
        }
        
        .download-section {
            background-color: white;
            border-radius: 10px;
            padding: 1.5rem;
            margin-top: 1.5rem;
            box-shadow: var(--shadow);
            text-align: center;
        }
        
        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: var(--primary-color);
        }
        
        .floating-alert {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1050;
            min-width: 300px;
            animation: slideIn 0.5s ease-out;
        }
        
        @keyframes slideIn {
            from {
                transform: translateX(100%);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }
        
        .pulse {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% {
                box-shadow: 0 0 0 0 rgba(44, 90, 160, 0.4);
            }
            70% {
                box-shadow: 0 0 0 10px rgba(44, 90, 160, 0);
            }
            100% {
                box-shadow: 0 0 0 0 rgba(44, 90, 160, 0);
            }
        }
        
        .process-section {
            margin-top: 2rem;
            padding: 1.5rem;
            background-color: #f8f9fa;
            border-radius: 10px;
        }
        
        .filter-tag {
            background-color: var(--primary-color);
            color: white;
            border-radius: 15px;
            padding: 2px 8px;
            font-size: 0.8rem;
            margin-left: 5px;
        }
        
                .tutorial-popup {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.7);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            animation: fadeIn 0.3s ease-out;
        }
        
        .tutorial-content {
            background-color: white;
            border-radius: 15px;
            width: 90%;
            max-width: 800px;
            max-height: 90vh;
            overflow-y: auto;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            animation: slideUp 0.4s ease-out;
        }
        
        .tutorial-header {
            background: var(--gradient-primary);
            color: white;
            padding: 1.5rem;
            position: relative;
        }
        
        .tutorial-header h4 {
            margin: 0;
            display: flex;
            align-items: center;
        }
        
        .tutorial-header h4 i {
            margin-right: 10px;
        }
        
        .close-tutorial {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(255, 255, 255, 0.2);
            border: none;
            border-radius: 50%;
            width: 30px;
            height: 30px;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .close-tutorial:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.1);
        }
        
        .tutorial-body {
            padding: 1.5rem;
        }
        
        .tutorial-body p {
            margin-bottom: 1rem;
            line-height: 1.6;
        }
        
        .tutorial-footer {
            padding: 1rem 1.5rem;
            background-color: #f8f9fa;
            display: flex;
            justify-content: space-between;
            border-top: 1px solid #dee2e6;
        }
        
        .tutorial-footer .btn {
            min-width: 120px;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        @keyframes slideUp {
            from { 
                opacity: 0;
                transform: translateY(30px);
            }
            to { 
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .tutorial-highlight {
            background-color: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 10px 15px;
            margin: 15px 0;
            border-radius: 0 5px 5px 0;
        }
        
        .video-container {
            position: relative;
            width: 100%;
            height: 0;
            padding-bottom: 56.25%;             margin: 1rem 0;
        }
        
        .video-container iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border-radius: 8px;
            border: none;
        }
        
        .tutorial-video-section {
            display: none;
        }
          .dropdown-menu-scroll {
    max-height: 250px;     overflow-y: auto;    }
    .card {
    border-radius: 1rem;
  }

    .filter-option {
    display: flex;
    align-items: center;
    background: #f9fafb;
    border: 1px solid #e2e6ea;
    border-radius: 0.6rem;
    padding: 0.75rem 1rem;
    transition: all 0.2s ease;
    cursor: pointer;
  }

  .filter-option:hover {
    background: #f0f4ff;
    border-color: #b6cffd;
  }

  .filter-option label {
    margin: 0;
    cursor: pointer;
    flex: 1;
    font-weight: 500;
    color: #333;
  }

  .filter-option .form-check-input {
    width: 1.1rem;
    height: 1.1rem;
    margin-right: 0.6rem;
    cursor: pointer;
  }

  .filter-option .form-check-input:checked {
    background-color: #0d6efd;
    border-color: #0d6efd;
  }

    .btn-modern {
    background: linear-gradient(135deg, #0d6efd, #007bff);
    border: none;
    border-radius: 50px;
    padding: 0.6rem 1.4rem;
    color: #fff;
    font-weight: 600;
    letter-spacing: 0.3px;
    transition: all 0.25s ease;
  }

  .btn-modern:hover {
    background: linear-gradient(135deg, #007bff, #0dcaf0);
    transform: translateY(-2px);
    box-shadow: 0 6px 14px rgba(13, 110, 253, 0.25);
  }

    @media (max-width: 768px) {
    .filter-option {
      padding: 0.65rem 0.9rem;
    }
  }
    </style>
</head>
<body>
    
    <div class="tutorial-popup" id="tutorialPopup">
        <div class="tutorial-content">
            <div class="tutorial-header">
                <h4><i class="fas fa-graduation-cap"></i> Tutorial Singkat</h4>
                <button class="close-tutorial" onclick="closeTutorial()">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <div class="tutorial-body">
                
                <div id="tutorialIntro">
                    <p>Selamat datang di sistem <strong>Filter Petani Belum Tebus</strong>. Ingin Menonton Video Tutorial?</p>
                </div>
                
                
                <div id="tutorialVideo" class="tutorial-video-section">
                    <h5><i class="fas fa-video me-2"></i>Video Tutorial</h5>
                    <div class="video-container">
                        <iframe id="tutorialIframe" src="https://www.youtube.com/embed/3jVLwsYHghk?si=86zW2aIRSvsgAkxE" allow="autoplay; encrypted-media" allowfullscreen></iframe>
                    </div>
                    <p class="text-muted small mt-2">Video akan diputar otomatis setelah Anda memilih "Putar Video"</p>
                </div>
            </div>
            <div class="tutorial-footer">
                <button class="btn btn-outline-secondary" id="skipTutorialBtn" onclick="skipTutorial()">
                    <i class="fas fa-forward me-1"></i> Skip
                </button>
                <button class="btn btn-primary" id="continueTutorialBtn" onclick="continueTutorial()">
                    <i class="fas fa-play me-1"></i> Lanjutkan
                </button>
                <button class="btn btn-success" id="startVideoBtn" onclick="startVideo()" style="display: none;">
                    <i class="fas fa-play-circle me-1"></i> Putar Video
                </button>
            </div>
        </div>
    </div>

    <div class="container-fluid py-4">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="card">
                    <div class="card-header text-white">
                        <h4 class="mb-0"><i class="fas fa-users-slash me-2"></i>Filter Petani Belum Tebus</h4>
                    </div>
                    <div class="card-body">
                        
                        <div id="alertContainer"></div>

                        
                        <div class="step-indicator">
                            <div class="step active" id="step1">
                                <div class="step-number">1</div>
                                <div class="step-label">Pilih Kondisi</div>
                            </div>
                            <div class="step" id="step2">
                                <div class="step-number">2</div>
                                <div class="step-label">Upload & Proses</div>
                            </div>
                            <div class="step" id="step3">
                                <div class="step-number">3</div>
                                <div class="step-label">Hasil & Download</div>
                            </div>
                        </div>
                        <div>
                            <p class="mb-1 text-dark p-3 rounded" style="background-color: #fff3cd;">
                                Sistem ini dibuat untuk membantu PPL dalam menyusun erdkk 2026
                                dengan menyajikan data petani tidak menebus tahun 2023 sd 2025. 
                                Diharapkan petani yang tidak pernah menebus / tidak aktif tidak masuk lagi sebagai 
                                daftar penerima pupuk subsidi 2026 sehingga data erdkk menjadi lebih valid dan tepat sasaran.
                            </p>
                        </div>

                        
                        <div class="condition-info p-3 mb-3" style="background: #f8f9fa; border-left: 4px solid #0d6efd; border-radius: 6px;">
                            <h6 class="mb-2 text-primary">
                                <i class="fas fa-info-circle me-2"></i>Informasi Penting
                            </h6>
                            <p class="mb-1 small">
                                Sistem ini berlaku untuk Wilayah Jateng 4:
                            </p>
                            <div class="p-2 mb-2 small fw-semibold text-dark" style="background: #d4edda; border: 1px solid #c3e6cb; border-radius: 4px;">
                                Magelang, Kota Magelang, Cilacap, Banyumas, Purbalingga, Banjarnegara, Kebumen, Purwerejo, Wonosobo, Temanggung
                            </div>
                            <p class="mb-0 small fst-italic text-muted">
                                Sistem akan menghapus data petani yang memenuhi kondisi <b>"Tidak Tebus"</b> pada tahun yang dipilih. 
                                Data akan dipertahankan jika tidak memenuhi kondisi atau tidak ditemukan di database.
                            </p>
                            <div class="dropdown mt-3">
                            <button class="btn btn-outline-secondary dropdown-toggle" type="button" id="dropdownDownload" data-bs-toggle="dropdown" aria-expanded="false">
                                Download Petani Belum Tebus
                            </button>
                            <ul class="dropdown-menu dropdown-menu-scroll" aria-labelledby="dropdownDownload">
                                    <li><a class="dropdown-item" href="https://docs.google.com/spreadsheets/d/1E2lx2LkrhAcEily1jDlrtKthDmhpBxdl/export?format=xlsx">Jateng 4</a></li>
                                </ul>
                            </div>
                        </div>

                        
                        <div id="step1-content" class="card shadow-sm border-0 mb-4">
                        <div class="card-body p-4">
                            <div class="d-flex align-items-center mb-3">
                            <div class="me-2 border-start border-4 border-primary rounded-end">&nbsp;</div>
                            <h5 class="fw-semibold text-dark mb-0 d-flex align-items-center">
                                <i class="fas fa-sliders-h text-primary me-2"></i> Pilih Kondisi Filter
                                <span id="selectedConditionsBadge" class="ms-2" style="display:none;">
                                <span id="conditionsCount" class="badge bg-light text-primary border border-primary">0 kondisi</span>
                                </span>
                            </h5>
                            </div>

                            <div class="row g-2">
                            <div class="col-md-4">
                                <div class="filter-option">
                                <input class="form-check-input condition-checkbox" type="checkbox" id="condition2023" value="2023">
                                <label class="form-check-label" for="condition2023">
                                    <i class="fas fa-calendar-xmark me-2 text-warning"></i>Tidak Tebus 2023
                                </label>
                                </div>
                            </div>

                            <div class="col-md-4">
                                <div class="filter-option">
                                <input class="form-check-input condition-checkbox" type="checkbox" id="condition2024" value="2024">
                                <label class="form-check-label" for="condition2024">
                                    <i class="fas fa-calendar-xmark me-2 text-warning"></i>Tidak Tebus 2024
                                </label>
                                </div>
                            </div>

                            <div class="col-md-4">
                                <div class="filter-option">
                                <input class="form-check-input condition-checkbox" type="checkbox" id="condition2025" value="2025">
                                <label class="form-check-label" for="condition2025">
                                    <i class="fas fa-calendar-xmark me-2 text-warning"></i>Tidak Tebus 2025
                                </label>
                                </div>
                            </div>
                            </div>

                            <small class="text-muted d-block mt-3">
                            <i class="fas fa-lightbulb text-warning me-1"></i>Pilih minimal satu kondisi untuk melanjutkan.
                            </small>

                            <div class="text-end mt-4">
                            <button type="button" id="nextToStep2" onclick="showStep(2)" class="btn btn-modern">
                                Lanjut ke Upload File <i class="fas fa-arrow-right ms-2"></i>
                            </button>
                            </div>
                        </div>
                        </div>

                        
                        <div class="mb-4" id="step2-content" style="display: none;">
                            <h5 class="mb-3">
                                <i class="fas fa-upload me-2"></i>Upload File Excel
                                <span id="step2ConditionsBadge" class="filter-tag">0 kondisi</span>
                            </h5>
                            
                            <div class="upload-area" id="uploadArea">
                                <i class="fas fa-file-excel fa-3x text-success mb-3"></i>
                                <h5>Seret file Excel ke sini atau klik untuk memilih</h5>
                                <p class="text-muted">Format file harus sesuai template (kolom KTP dan Nama Petani harus ada)</p>
                                <input type="file" id="fileInput" accept=".xlsx,.xls" class="d-none">
                                <button type="button" class="btn btn-outline-primary mt-2 pulse" onclick="document.getElementById('fileInput').click()">
                                    <i class="fas fa-folder-open me-2"></i>Pilih File
                                </button>
                            </div>
                            
                            <div id="fileInfo" class="mt-3" style="display: none;">
                                <div class="alert alert-info d-flex justify-content-between align-items-center">
                                    <span>
                                        <i class="fas fa-file me-2"></i>
                                        <span id="fileName" class="fw-bold"></span>
                                        <small class="d-block text-muted" id="fileSize"></small>
                                    </span>
                                    <button type="button" class="btn-close" onclick="clearFile()"></button>
                                </div>
                            </div>
                            <div class="alert alert-warning d-flex align-items-center p-2 mb-3" role="alert">
                                <i class="fas fa-exclamation-triangle me-2"></i>
                                <div class="small">
                                    Ukuran file upload maksimal <b>20 MB</b>. Pastikan juga file sesuai template sebelum diunggah.
                                </div>
                            </div>

                            <div class="mt-3">
                                <a href="/api/jateng/template" class="btn btn-outline-success btn-sm">
                                    <i class="fas fa-download me-2"></i>Download Template
                                </a>
                                <small class="text-muted ms-2">Template sudah termasuk kolom "Nama Penyuluh" sebagai kolom pertama</small>
                            </div>
                            
                            
                            <div class="process-section" id="processSection" style="display: none;">
                                <h5 class="mb-3"><i class="fas fa-cogs me-2"></i>Proses Filter</h5>
                                
                                <div class="d-grid gap-2">
                                    <button type="button" id="processBtn" class="btn btn-primary btn-lg" onclick="processRemove()">
                                        <i class="fas fa-play me-2"></i>Proses Filter
                                    </button>
                                </div>

                                
                                <div id="progressContainer" class="mt-3" style="display: none;">
                                    <div class="progress" style="height: 20px;">
                                        <div id="progressBar" class="progress-bar progress-bar-striped progress-bar-animated" 
                                             role="progressbar" style="width: 0%"></div>
                                    </div>
                                    <div id="progressText" class="text-center mt-2 fw-bold"></div>
                                </div>
                            </div>
                            
                            <div class="mt-4 d-flex justify-content-between">
                                <button type="button" class="btn btn-outline-secondary" onclick="showStep(1)">
                                    <i class="fas fa-arrow-left me-2"></i>Kembali ke Pilih Kondisi
                                </button>
                            </div>
                        </div>

                        
                        <div id="step3-content" style="display: none;">
                            <div class="stats-card">
                                <h5><i class="fas fa-check-circle me-2"></i>Proses Filter Selesai!</h5>
                                <div id="resultsStats" class="row mt-3"></div>
                            </div>
                            
                            
                            <div class="download-section text-center">
                                <div class="download-header mb-4">
                                    <i class="fas fa-cloud-download-alt feature-icon text-primary mb-3"></i>
                                    <h4>File Hasil Siap Diunduh</h4>
                                    <p class="text-muted">Pilih file yang Anda butuhkan</p>
                                </div>
                                
                                <div class="download-options row g-3 justify-content-center">
                                    
                                    <div class="col-md-5">
                                        <div class="download-card card h-100 border-0 shadow-sm">
                                            <div class="card-body text-center p-4">
                                                <div class="download-icon mb-3">
                                                    <i class="fas fa-check-circle text-success fa-3x"></i>
                                                </div>
                                                <h5 class="card-title">Data Bersih</h5>
                                                <p class="card-text text-muted small">
                                                    Data petani yang lolos filter dan siap digunakan
                                                </p>
                                                <div class="download-info mb-3">
                                                    <span class="badge bg-success bg-opacity-10 text-success">
                                                        <i class="fas fa-file-excel me-1"></i>
                                                        Excel File
                                                    </span>
                                                </div>
                                                <button type="button" id="downloadBtn" class="btn btn-success w-100">
                                                    <i class="fas fa-download me-2"></i>Download
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                    
                                    
                                    <div class="col-md-5">
                                        <div class="download-card card h-100 border-0 shadow-sm">
                                            <div class="card-body text-center p-4">
                                                <div class="download-icon mb-3">
                                                    <i class="fas fa-times-circle text-danger fa-3x"></i>
                                                </div>
                                                <h5 class="card-title">Data Dihapus</h5>
                                                <p class="card-text text-muted small">
                                                    Data petani yang dihapus
                                                </p>
                                                <div class="download-info mb-3">
                                                    <span class="badge bg-danger bg-opacity-10 text-danger">
                                                        <i class="fas fa-file-excel me-1"></i>
                                                        Excel File
                                                    </span>
                                                </div>
                                                <button type="button" id="downloadRemovedBtn" class="btn btn-outline-danger w-100">
                                                    <i class="fas fa-download me-2"></i>Download
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="download-footer mt-4">
                                    <p class="small text-muted">
                                        <i class="fas fa-info-circle me-1"></i>
                                        File akan otomatis terhapus dalam 5 menit setelah diunduh
                                    </p>
                                </div>
                            </div>
                            
                            
                            <div class="text-center mt-5">
                                <button type="button" class="btn btn-outline-primary" onclick="showStep(1)">
                                    <i class="fas fa-arrow-left me-2"></i>Ulangi Proses
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="text-center py-3 mt-4" style="background-color: #f8f9fa; border-top: 1px solid #dee2e6;">
        <small class="text-muted">© 2025 Part of <a href="https://pi-mon.com" target="_blank" class="text-decoration-none fw-bold">Pi-Mon.com</a> 
        | Developed By <a href="https://ihpazlagasukanda.vercel.app">ILS</a></small>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        let selectedFile = null;
        let downloadUrl = null;
        let removedDataUrl = null;
        let selectedConditions = [];
        let tutorialVideoUrl = "https://www.youtube.com/embed/iOVvcWEDzLo?si=Rf4WKrie0N2lElFG"; 
        let videoStarted = false; 

        
        function showTutorialPopup() {
            
            document.getElementById('tutorialIntro').style.display = 'block';
            document.getElementById('tutorialVideo').style.display = 'none';
            document.getElementById('continueTutorialBtn').style.display = 'block';
            document.getElementById('startVideoBtn').style.display = 'none';
            
            
            resetVideo();
            
            
            document.getElementById('tutorialPopup').style.display = 'flex';
        }
        
        function closeTutorial() {
            
            resetVideo();
            
            
            document.getElementById('tutorialPopup').style.display = 'none';
            localStorage.setItem('tutorialClosed', 'true');
        }
        
        function skipTutorial() {
            closeTutorial();
            
        }
        
        function continueTutorial() {
            
            document.getElementById('tutorialIntro').style.display = 'none';
            document.getElementById('tutorialVideo').style.display = 'block';
            
            
            document.getElementById('continueTutorialBtn').style.display = 'none';
            document.getElementById('startVideoBtn').style.display = 'block';
        }
        
        function startVideo() {
            
            const iframe = document.getElementById('tutorialIframe');
            iframe.src = tutorialVideoUrl + "&autoplay=1&mute=0";
            
            
            videoStarted = true;
            
            
            document.getElementById('startVideoBtn').innerHTML = '<i class="fas fa-pause-circle me-1"></i> Jeda Video';
            document.getElementById('startVideoBtn').setAttribute('onclick', 'pauseVideo()');
            
            
            
            
            
        }
        
        function pauseVideo() {
            
            const iframe = document.getElementById('tutorialIframe');
            iframe.src = tutorialVideoUrl;
            
            
            videoStarted = false;
            
            
            document.getElementById('startVideoBtn').innerHTML = '<i class="fas fa-play-circle me-1"></i> Putar Video';
            document.getElementById('startVideoBtn').setAttribute('onclick', 'startVideo()');
        }
        
        function resetVideo() {
            
            const iframe = document.getElementById('tutorialIframe');
            iframe.src = "";
            
            
            videoStarted = false;
        }

        
        function showStep(stepNumber) {
            
            if (stepNumber === 2) {
                const conditions = document.querySelectorAll('.condition-checkbox:checked');
                if (conditions.length === 0) {
                    showAlert('Pilih minimal satu kondisi filter', 'warning');
                    return;
                }
            }
            
            
            document.getElementById('step1-content').style.display = 'none';
            document.getElementById('step2-content').style.display = 'none';
            document.getElementById('step3-content').style.display = 'none';
            
            
            document.querySelectorAll('.step').forEach(step => {
                step.classList.remove('active', 'completed');
            });
            
            
            document.getElementById(`step${stepNumber}-content`).style.display = 'block';
            
            
            for (let i = 1; i <= 3; i++) {
                const stepElement = document.getElementById(`step${i}`);
                if (i < stepNumber) {
                    stepElement.classList.add('completed');
                } else if (i === stepNumber) {
                    stepElement.classList.add('active');
                }
            }
            
            
            updateConditionsDisplay();
            
            
            if (stepNumber === 1 && !event.target.classList.contains('btn-outline-secondary')) {
                
                const isFromStep3 = document.getElementById('step3-content').style.display !== 'none';
                if (!isFromStep3) {
                    
                    resetStep1Only();
                }
            }
        }

        
        function updateConditionsDisplay() {
            const selectedCount = selectedConditions.length;
            const conditionsText = `${selectedCount} kondisi`;
            
            
            const badge1 = document.getElementById('selectedConditionsBadge');
            const count1 = document.getElementById('conditionsCount');
            if (selectedCount > 0) {
                badge1.style.display = 'inline';
                count1.textContent = conditionsText;
            } else {
                badge1.style.display = 'none';
            }
            
            
            const badge2 = document.getElementById('step2ConditionsBadge');
            badge2.textContent = conditionsText;
            
            
            const conditionsList = document.getElementById('step2ConditionsList');
            if (conditionsList) {
                conditionsList.innerHTML = selectedConditions.map(cond => 
                    `<span class="badge bg-primary me-1">${cond}</span>`
                ).join('');
            }
        }

        
        function saveSelectedConditions() {
            selectedConditions = Array.from(document.querySelectorAll('.condition-checkbox:checked'))
                                   .map(cb => cb.value);
            
            
            localStorage.setItem('selectedConditions', JSON.stringify(selectedConditions));
        }

        
        function loadSelectedConditions() {
            const savedConditions = localStorage.getItem('selectedConditions');
            if (savedConditions) {
                selectedConditions = JSON.parse(savedConditions);
                
                
                document.querySelectorAll('.condition-checkbox').forEach(checkbox => {
                    checkbox.checked = selectedConditions.includes(checkbox.value);
                });
            }
            updateConditionsDisplay();
        }

        
        function resetStep1Only() {
            
            selectedFile = null;
            downloadUrl = null;
            fileInput.value = '';
            
            
            document.getElementById('fileInfo').style.display = 'none';
            document.getElementById('processSection').style.display = 'none';
            document.getElementById('progressContainer').style.display = 'none';
        }

        
        function resetForm() {
            
            selectedFile = null;
            downloadUrl = null;
            removedDataUrl = null;
            selectedConditions = [];
            fileInput.value = '';
            
            
            document.getElementById('fileInfo').style.display = 'none';
            document.getElementById('progressContainer').style.display = 'none';
            document.getElementById('processSection').style.display = 'none';
            
            
            document.querySelectorAll('.condition-checkbox').forEach(checkbox => {
                checkbox.checked = false;
            });
            
            
            localStorage.removeItem('selectedConditions');
            
            
            updateConditionsDisplay();
            
            
            showStep(1);
            
            showAlert('Form telah direset. Silakan mulai proses baru.', 'info');
        }

        
        const uploadArea = document.getElementById('uploadArea');
        const fileInput = document.getElementById('fileInput');

        uploadArea.addEventListener('dragover', (e) => {
            e.preventDefault();
            uploadArea.classList.add('dragover');
        });

        uploadArea.addEventListener('dragleave', () => {
            uploadArea.classList.remove('dragover');
        });

        uploadArea.addEventListener('drop', (e) => {
            e.preventDefault();
            uploadArea.classList.remove('dragover');
            if (e.dataTransfer.files.length > 0) {
                handleFileSelect(e.dataTransfer.files[0]);
            }
        });

        fileInput.addEventListener('change', (e) => {
            if (e.target.files.length > 0) {
                handleFileSelect(e.target.files[0]);
            }
        });

        function handleFileSelect(file) {
            if (!file.name.match(/\.(xlsx|xls)$/)) {
                showAlert('Hanya file Excel (.xlsx, .xls) yang diperbolehkan', 'danger');
                return;
            }

            
            if (file.size > 10 * 1024 * 1024) {
                showAlert('Ukuran file maksimal 10MB', 'danger');
                return;
            }

            selectedFile = file;
            document.getElementById('fileName').textContent = file.name;
            document.getElementById('fileSize').textContent = `Size: ${(file.size / 1024 / 1024).toFixed(2)} MB`;
            document.getElementById('fileInfo').style.display = 'block';
            
            
            document.getElementById('processSection').style.display = 'block';
        }

        function clearFile() {
            selectedFile = null;
            fileInput.value = '';
            document.getElementById('fileInfo').style.display = 'none';
            document.getElementById('processSection').style.display = 'none';
        }

        
        document.querySelectorAll('.condition-checkbox').forEach(checkbox => {
            checkbox.addEventListener('change', function() {
                
                
                
                
                
                
                
                saveSelectedConditions();
                updateConditionsDisplay();
            });
        });

        function showAlert(message, type) {
            
            const existingAlert = document.querySelector('.floating-alert');
            if (existingAlert) {
                existingAlert.remove();
            }
            
            const alert = document.createElement('div');
            alert.className = `alert alert-${type} alert-dismissible fade show floating-alert`;
            alert.innerHTML = `
                <i class="fas fa-${type === 'success' ? 'check-circle' : type === 'warning' ? 'exclamation-triangle' : 'exclamation-circle'} me-2"></i>
                ${message}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            `;
            document.body.appendChild(alert);
            
            
            setTimeout(() => {
                if (alert.parentElement) {
                    alert.remove();
                }
            }, 5000);
        }

        async function processRemove() {
            
            const conditions = selectedConditions;

            
            if (conditions.length === 0) {
                showAlert('Pilih minimal satu kondisi filter', 'warning');
                return;
            }

            
            
            
            

            if (!selectedFile) {
                showAlert('Pilih file Excel terlebih dahulu', 'warning');
                return;
            }

            const progressContainer = document.getElementById('progressContainer');
            const progressBar = document.getElementById('progressBar');
            const progressText = document.getElementById('progressText');
            const processBtn = document.getElementById('processBtn');
            
            progressContainer.style.display = 'block';
            processBtn.disabled = true;
            progressBar.style.width = '30%';
            progressText.textContent = 'Mengupload file...';

            const formData = new FormData();
            formData.append('file', selectedFile);
            formData.append('conditions', conditions.join(','));

            try {
                progressBar.style.width = '60%';
                progressText.textContent = 'Memproses data...';

                const response = await fetch('/api/jateng', {
                    method: 'POST',
                    body: formData
                });

                console.log('Response status:', response.status);
                
                
                const contentType = response.headers.get('content-type');
                if (!contentType || !contentType.includes('application/json')) {
                    const text = await response.text();
                    
                    if (text.includes('<!DOCTYPE html>') || text.includes('<html')) {
                        const errorMatch = text.match(/<title[^>]*>(.*?)<\/title>/) || 
                                         text.match(/<h1[^>]*>(.*?)<\/h1>/) ||
                                         text.match(/<p[^>]*>(.*?)<\/p>/);
                        
                        const errorMessage = errorMatch ? errorMatch[1] : 'Server mengembalikan halaman error';
                        
                        throw new Error(`Server Error: ${errorMessage} (Status: ${response.status})`);
                    }
                    
                    throw new Error(`Server mengembalikan format tidak valid. Status: ${response.status}`);
                }

                const result = await response.json();

                if (!response.ok) {
                    throw new Error(result.error || `HTTP error! status: ${response.status}`);
                }

                progressBar.style.width = '100%';
                progressText.textContent = 'Selesai!';

                if (result.success) {
                    
                    downloadUrl = result.download_url;
                    removedDataUrl = result.removed_data_url;
                    
                    
                    showResults(result);
                    
                    
                    setTimeout(() => {
                        showStep(3);
                    }, 1000);
                } else {
                    throw new Error(result.error || 'Terjadi kesalahan');
                }

            } catch (error) {
                console.error('Process error:', error);
                
                let errorMessage = error.message;
                if (error.message.includes('Server mengembalikan format tidak valid')) {
                    errorMessage = 'Server sedang mengalami masalah. Silakan coba lagi atau hubungi administrator.';
                }
                
                showAlert('Error: ' + errorMessage, 'danger');
                
                
                progressContainer.style.display = 'none';
                processBtn.disabled = false;
                progressBar.style.width = '0%';
            }
        }

        function showResults(result) {
            const resultsStats = document.getElementById('resultsStats');

            
            resultsStats.innerHTML = `
                <div class="col-md-4 text-center">
                    <h3>${result.stats.total_data_file}</h3>
                    <small>Total Data/Baris dalam File</small>
                </div>
                <div class="col-md-4 text-center">
                    <h3 class="status-success">${result.stats.data_dipertahankan}</h3>
                    <small>Data Dipertahankan</small>
                </div>
                <div class="col-md-4 text-center">
                    <h3 class="status-danger">${result.stats.data_dihapus}</h3>
                    <small>Data Dihapus Per NIK/Petani</small>
            `;

            
            const conditionInfo = document.createElement('div');
            conditionInfo.className = 'col-12 mt-3';
            conditionInfo.innerHTML = `
                <div class="alert alert-light">
                    <strong>Kondisi yang diterapkan:</strong> ${result.stats.kondisi_diterapkan.join(', ')}
                </div>
            `;
            resultsStats.appendChild(conditionInfo);
        }

        
        document.getElementById('downloadBtn').addEventListener('click', function() {
            if (downloadUrl) {
                
                window.open(downloadUrl, '_blank');
                
                
                showAlert('File hasil berhasil diunduh!', 'success');
            } else {
                showAlert('Tidak ada file yang tersedia untuk diunduh', 'warning');
            }
        });

        document.getElementById('downloadRemovedBtn').addEventListener('click', function() {
            if (removedDataUrl) {
                
                window.open(removedDataUrl, '_blank');
                
                
                showAlert('File data yang dihapus berhasil diunduh!', 'success');
            } else {
                showAlert('Tidak ada file data yang dihapus tersedia', 'warning');
            }
        });

        
        document.addEventListener('keydown', (e) => {
            
            if (e.ctrlKey && e.key === 'r') {
                e.preventDefault();
                resetForm();
            }
            
            
            if (e.ctrlKey && e.key === 'Enter') {
                e.preventDefault();
                const processBtn = document.getElementById('processBtn');
                if (!processBtn.disabled) {
                    processRemove();
                }
            }
            
            
            if (e.key === 'Escape') {
                const tutorialPopup = document.getElementById('tutorialPopup');
                if (tutorialPopup.style.display === 'flex') {
                    closeTutorial();
                }
            }
        });

        
        window.addEventListener('load', async () => {
            try {
                const response = await fetch('/api/jateng/health');
                if (response.ok) {
                    console.log('✅ Backend API is running');
                }
            } catch (error) {
                console.warn('⚠️ Backend health check failed:', error);
            }
            
            
            loadSelectedConditions();
            
            
            showStep(1);
            
            
            
            const tutorialClosed = localStorage.getItem('tutorialClosed');
            if (!tutorialClosed) {
                showTutorialPopup();
            }
        });
    </script>
</body>
</html>