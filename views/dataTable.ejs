<!DOCTYPE html>
<html>
  <head>
    <%- include('./partials/head'); %>
    <style>
      .processing-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0,0,0,0.5);
        z-index: 9999;
        display: flex;
        justify-content: center;
        align-items: center;
        color: white;
        font-size: 1.5rem;
      }
      .spinner {
        width: 40px;
        height: 40px;
        border: 4px solid #f3f3f3;
        border-top: 4px solid #3498db;
        border-radius: 50%;
        animation: spin 1s linear infinite;
        margin-right: 10px;
      }
      @keyframes spin {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
      }
      .filter-section {
        background-color: #f8f9fa;
        padding: 15px;
        border-radius: 5px;
        margin-bottom: 20px;
      }
      .filter-section h5 {
        margin-bottom: 15px;
        color: #495057;
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <%- include('./partials/header'); %>

      <div class="container">
        <div class="page-inner">
          <div class="page-header">
            <h3 class="fw-bold mb-3">Data Verval</h3>
            <ul class="breadcrumbs mb-3">
              <li class="nav-home">
                <a href="/">
                  <i class="icon-home"></i>
                </a>
              </li>
              <li class="separator">
                <i class="icon-arrow-right"></i>
              </li>
              <li class="nav-item">
                <a href="/dataverval">Data</a>
              </li>
              <li class="separator">
                <i class="icon-arrow-right"></i>
              </li>
              <li class="nav-item">
                <a href="/dataverval">Data Verval</a>
              </li>
            </ul>
          </div>

          <div class="filter-section">
            <h5>Filter Data</h5>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 mb-3">
              <!-- Kabupaten -->
              <div class="grid grid-cols-2 items-center gap-2">
                <label for="filter-kabupaten" class="text-sm font-medium text-gray-700">Kabupaten</label>
                <select id="filter-kabupaten" class="filter-dropdown border border-gray-300 rounded-lg shadow-sm p-2 h-[40px] w-full">
                  <% if (user.kabupaten && user.kabupaten !== 'ALL') { %>
                      <option value="">Pilih Kabupaten</option>
                      <option value="<%= user.kabupaten %>"><%= user.kabupaten %></option>
                    <% } else { %>
                  <option value="">Pilih Kabupaten</option>
                                <option value="BOYOLALI">BOYOLALI</option>
                                <option value="SRAGEN">SRAGEN</option>
                                <option value="KOTA SURAKARTA">KOTA SURAKARTA</option>
                                <option value="KOTA MAGELANG">KOTA MAGELANG</option>
                                <option value="MAGELANG">MAGELANG</option>
                                <option value="SUKOHARJO">SUKOHARJO</option>
                                <option value="KLATEN">KLATEN</option>
                                <option value="WONOGIRI">WONOGIRI</option>
                                <option value="KARANGANYAR">KARANGANYAR</option>
                                <option value="BANTUL">BANTUL</option>
                                <option value="KOTA YOGYAKARTA">KOTA YOGYAKARTA</option>
                                <option value="SLEMAN">SLEMAN</option>
                                <option value="KULON PROGO">KULON PROGO</option>
                                <option value="GUNUNG KIDUL">GUNUNG KIDUL</option>
                  <% } %>
                </select>
              </div>
            
              <!-- Kecamatan -->
              <div class="grid grid-cols-2 items-center gap-2">
                <label for="filter-kecamatan" class="text-sm font-medium text-gray-700">Kecamatan</label>
                <select id="filter-kecamatan" class="filter-dropdown border border-gray-300 rounded-lg shadow-sm p-2 h-[40px] w-full">
                  <option value="">Pilih Kecamatan</option>
                </select>
              </div>
            
              <!-- Metode Penebusan -->
              <div class="grid grid-cols-2 items-center gap-2">
                <label for="filter-metode-penebusan" class="text-sm font-medium text-gray-700">Metode Penebusan</label>
                <select id="filter-metode-penebusan" class="filter-dropdown border border-gray-300 rounded-lg shadow-sm p-2 h-[40px] w-full">
                  <option value="">Pilih Metode</option>
                </select>
              </div>
            </div>
            
            <!-- Kelompok Tahun, Bulan, dan Periode Bulan -->
            <div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-3 gap-4 mb-3">
              <!-- Tahun -->
              <div class="grid grid-cols-2 items-center gap-2">
                <label for="filter-tahun" class="text-sm font-medium text-gray-700">Tahun</label>
                <select id="filter-tahun" class="border border-gray-300 rounded-lg shadow-sm p-2 h-[40px] w-full">
                  <option value="">Pilih Tahun</option>
                </select>
              </div>
            
              <!-- Bulan -->
              <div class="grid grid-cols-2 items-center gap-2">
                <label for="filter-bulan" class="text-sm font-medium text-gray-700">Bulan</label>
                <select id="filter-bulan" class="border border-gray-300 rounded-lg shadow-sm p-2 h-[40px] w-full">
                  <option value="">Pilih Bulan</option>
                  <option value="01">Januari</option>
                  <option value="02">Februari</option>
                  <option value="03">Maret</option>
                  <option value="04">April</option>
                  <option value="05">Mei</option>
                  <option value="06">Juni</option>
                  <option value="07">Juli</option>
                  <option value="08">Agustus</option>
                  <option value="09">September</option>
                  <option value="10">Oktober</option>
                  <option value="11">November</option>
                  <option value="12">Desember</option>
                </select>
              </div>
            
              <!-- Periode Bulan -->
              <div class="grid grid-cols-3 items-center gap-2">
                <label class="text-sm font-medium text-gray-700">Periode Bulan:</label>
                <select id="periode-bulan-awal" class="border border-gray-300 rounded-lg shadow-sm p-2 h-[40px] w-full">
                  <option value="">Bulan Awal</option>
                  <option value="01">Januari</option>
                  <option value="02">Februari</option>
                  <option value="03">Maret</option>
                  <option value="04">April</option>
                  <option value="05">Mei</option>
                  <option value="06">Juni</option>
                  <option value="07">Juli</option>
                  <option value="08">Agustus</option>
                  <option value="09">September</option>
                  <option value="10">Oktober</option>
                  <option value="11">November</option>
                  <option value="12">Desember</option>
                </select>
                <span class="text-center">-</span>
                <select id="periode-bulan-akhir" class="border border-gray-300 rounded-lg shadow-sm p-2 h-[40px] w-full">
                  <option value="">Bulan Akhir</option>
                  <option value="01">Januari</option>
                  <option value="02">Februari</option>
                  <option value="03">Maret</option>
                  <option value="04">April</option>
                  <option value="05">Mei</option>
                  <option value="06">Juni</option>
                  <option value="07">Juli</option>
                  <option value="08">Agustus</option>
                  <option value="09">September</option>
                  <option value="10">Oktober</option>
                  <option value="11">November</option>
                  <option value="12">Desember</option>
                </select>
              </div>
            </div>
            

            <!-- Tombol Muat dan Reset dengan Bootstrap -->
<div class="d-flex gap-2 mt-2">
  <button id="load-data-btn" class="btn btn-primary">
    Terapkan Filter
  </button>
  <button id="reset-filters" class="btn btn-secondary">
    Reset
  </button>
</div>

          </div>

          <div class="d-flex align-items-left align-items-md-center flex-column flex-md-row pt-2 pb-4">
            <div>
              <p class="op-7 mb-2">Data akan ditampilkan setelah filter dipilih dan tombol "Muat Data" diklik</p>
            </div>
            <div class="ms-md-auto py-2 py-md-0">
              <button id="download-excel" class="btn btn-success btn-round" disabled>
                <i class="fas fa-file-excel"></i> Download Excel
              </button>
            </div>
          </div>

          <div class="row">
            <div class="col-md-12">
              <div class="card">
                <div class="card-header">
                  <div class="d-flex justify-content-between align-items-center">
                    <h4 class="card-title">Verval</h4>
                    <div id="data-info" class="text-muted">
                      Data belum dimuat
                    </div>
                  </div>
                </div>
                <div class="card-body">
                  <div class="table-responsive">
                    <table id="multi-filter-select" class="table">
                      <thead>
                        <tr>
                          <th>Kabupaten</th>
                          <th>Kecamatan</th>
                          <th>Kode Kios</th>
                          <th>Nama Kios</th>
                          <th>NIK</th>
                          <th>Nama Petani</th>
                          <th>Metode Penebusan</th>
                          <th>Tanggal Tebus</th>
                          <th>Urea</th>
                          <th>NPK</th>
                          <th>SP36</th>
                          <th>ZA</th>
                          <th>NPK Formula</th>
                          <th>Organik</th>
                          <th>Organik Cair</th>
                          <th>Kakao</th>
                          <th>Status</th>
                        </tr>
                      </thead>
                      <tfoot>
                        <tr>
                          <th id="sum-kabupaten">-</th>
                          <th id="sum-kecamatan">-</th>
                          <th>-</th>
                          <th>-</th>
                          <th>-</th>
                          <th>-</th>
                          <th id="sum-metode">-</th>
                          <th id="sum-tanggal">-</th>
                          <th id="sum-urea-text">0</th>
                          <th id="sum-npk-text">0</th>
                          <th id="sum-sp36-text">0</th>
                          <th id="sum-za-text">0</th>
                          <th id="sum-npk-formula-text">0</th>
                          <th id="sum-organik-text">0</th>
                          <th id="sum-organik-cair-text">0</th>
                          <th id="sum-kakao-text">0</th>
                          <th>-</th>
                        </tr>
                      </tfoot>
                      <tbody>
                        <!-- Data akan diisi oleh DataTables -->
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <%- include('./partials/footer'); %>
    </div>

    <!-- Processing Overlay -->
    <div id="processing-overlay" class="processing-overlay" style="display: none;">
      <div class="spinner"></div>
      <span>Memproses data...</span>
    </div>

    <!-- Core JS Files -->
    <script src="./assets/js/core/jquery-3.7.1.min.js"></script>
    <script src="./assets/js/core/popper.min.js"></script>
    <script src="./assets/js/core/bootstrap.min.js"></script>
    <script src="./assets/js/plugin/jquery-scrollbar/jquery.scrollbar.min.js"></script>
    <script src="./assets/js/plugin/datatables/datatables.min.js"></script>
    <script src="./assets/js/kaiadmin.min.js"></script>
    <script src="./assets/js/setting-demo2.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.0.13/dist/js/select2.min.js"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

    <script>
      $(document).ready(function () {
        let dataTable;
        const processingOverlay = $('#processing-overlay');
        let isInitialLoad = true;
        let hasData = false;

        // Fungsi untuk menampilkan loading
        function showProcessing() {
          processingOverlay.show();
        }

        // Fungsi untuk menyembunyikan loading
        function hideProcessing() {
          processingOverlay.hide();
        }

        // Isi dropdown tahun dari 2010 sampai sekarang
        const tahunSelect = document.getElementById("filter-tahun");
        const tahunSekarang = new Date().getFullYear();
        for (let tahun = 2024; tahun <= tahunSekarang; tahun++) {
          const option = document.createElement("option");
          option.value = tahun;
          option.textContent = tahun;
          tahunSelect.appendChild(option);
        }

        // Inisialisasi DataTable tanpa memuat data
        function initializeDataTable() {
          dataTable = $('#multi-filter-select').DataTable({
            processing: true,
            serverSide: true,
            deferLoading: 0,
            ajax: {
              url: '/api/data',
              type: 'GET',
              data: function (d) {
                // Ambil nilai filter
                d.kabupaten = $('#filter-kabupaten').val();
                d.kecamatan = $('#filter-kecamatan').val();
                d.metode_penebusan = $('#filter-metode-penebusan').val();
                
                // Handle filter tanggal
                const bulan = $('#filter-bulan').val();
                const tahun = $('#filter-tahun').val();
                const bulanAwal = $('#periode-bulan-awal').val();
                const bulanAkhir = $('#periode-bulan-akhir').val();

                if (bulanAwal && bulanAkhir && tahun) {
                  d.bulan_awal = `${tahun}-${bulanAwal}`;
                  d.bulan_akhir = `${tahun}-${bulanAkhir}`;
                } else if (bulan && tahun) {
                  d.bulan = bulan;
                  d.tahun = tahun;
                } else if (tahun) {
                  d.tahun = tahun;
                }
              },
              beforeSend: function() {
                showProcessing();
              },
              complete: function() {
                hideProcessing();
              },
              error: function(xhr, error, thrown) {
                hideProcessing();
                console.error('Error loading data:', error);
                alert('Terjadi kesalahan saat memuat data. Silakan coba lagi.');
              }
            },
            columns: [
              { data: 'kabupaten' },
              { data: 'kecamatan' },
              { data: 'kode_kios' },
              { data: 'nama_kios' },
              { data: 'nik' },
              { data: 'nama_petani' },
              { data: 'metode_penebusan' },
              { 
                data: 'tanggal_tebus',
                render: function(data) {
                  return new Date(data).toLocaleDateString('id-ID');
                }
              },
              { data: 'urea', render: $.fn.dataTable.render.number(',', '.', 0, '') },
              { data: 'npk', render: $.fn.dataTable.render.number(',', '.', 0, '') },
              { data: 'sp36', render: $.fn.dataTable.render.number(',', '.', 0, '') },
              { data: 'za', render: $.fn.dataTable.render.number(',', '.', 0, '') },
              { data: 'npk_formula', render: $.fn.dataTable.render.number(',', '.', 0, '') },
              { data: 'organik', render: $.fn.dataTable.render.number(',', '.', 0, '') },
              { data: 'organik_cair', render: $.fn.dataTable.render.number(',', '.', 0, '') },
              { data: 'kakao', render: $.fn.dataTable.render.number(',', '.', 0, '') },
              { data: 'status' }
            ],
            language: {
              processing: "Memproses...",
              search: "Cari:",
              lengthMenu: "Tampilkan _MENU_ data per halaman",
              info: "Menampilkan _START_ sampai _END_ dari _TOTAL_ data",
              infoEmpty: "Menampilkan 0 sampai 0 dari 0 data",
              infoFiltered: "(disaring dari _MAX_ total data)",
              zeroRecords: "Tidak ditemukan data yang sesuai",
              paginate: {
                first: "Pertama",
                last: "Terakhir",
                next: "Selanjutnya",
                previous: "Sebelumnya"
              }
            },
            initComplete: function() {
              hideProcessing();
              if (hasData) {
                updateSum();
                updateDataInfo();
              }
            },
            drawCallback: function() {
              hideProcessing();
              if (hasData) {
                updateSum();
                updateDataInfo();
              }
            }
          });
          
          // Nonaktifkan ajax saat inisialisasi pertama
          dataTable.ajax.url(null);
        }

        // Fungsi untuk memuat data
        function loadData() {
        const tahun = $('#filter-tahun').val();
        const bulan = $('#filter-bulan').val();
        const bulanAwal = $('#periode-bulan-awal').val();
        const bulanAkhir = $('#periode-bulan-akhir').val();

        const isPeriodeFilled = bulanAwal || bulanAkhir;
        const isPeriodeComplete = bulanAwal && bulanAkhir;

        // Wajib pilih tahun
        if (!tahun) {
          alert('Harap pilih tahun terlebih dahulu');
          return;
        }

        // Kalau periode diisi sebagian, kasih peringatan
        if (isPeriodeFilled && !isPeriodeComplete) {
          alert('Harap lengkapi periode bulan (awal dan akhir)');
          return;
        }

        // Kalau periode lengkap, validasi bulan awal <= akhir
        if (isPeriodeComplete && bulanAwal > bulanAkhir) {
          alert('Bulan awal tidak boleh lebih besar dari bulan akhir');
          return;
        }

        // Load data
        if (dataTable) {
          isInitialLoad = false;
          hasData = true;
          $('#download-excel').prop('disabled', false);
          dataTable.ajax.url('/api/data').load();
          updateSum();
          updateDataInfo();
        }
        }


        // Fungsi untuk update info data
        function updateDataInfo() {
          const kabupaten = $('#filter-kabupaten').val() || 'Semua Kabupaten';
          const kecamatan = $('#filter-kecamatan').val() || 'Semua Kecamatan';
          const metode = $('#filter-metode-penebusan').val() || 'Semua Metode';
          const tahun = $('#filter-tahun').val();
          const bulan = $('#filter-bulan').val();
          const bulanAwal = $('#periode-bulan-awal').val();
          const bulanAkhir = $('#periode-bulan-akhir').val();
          
          let periodeText = '';
          if (bulanAwal && bulanAkhir && tahun) {
            periodeText = `Periode ${bulanAwal}-${bulanAkhir} ${tahun}`;
          } else if (bulan && tahun) {
            const bulanNames = ['Januari', 'Februari', 'Maret', 'April', 'Mei', 'Juni', 'Juli', 'Agustus', 'September', 'Oktober', 'November', 'Desember'];
            periodeText = `${bulanNames[parseInt(bulan)-1]} ${tahun}`;
          } else if (tahun) {
            periodeText = `Tahun ${tahun}`;
          }
          
          $('#data-info').text(`${kabupaten} - ${kecamatan} - ${metode} - ${periodeText}`);
        }

        // Fungsi untuk update total
        function updateSum() {
          showProcessing();
          
          const params = {
            kabupaten: $('#filter-kabupaten').val(),
            kecamatan: $('#filter-kecamatan').val(),
            metode_penebusan: $('#filter-metode-penebusan').val(),
            tahun: $('#filter-tahun').val(),
            bulan: $('#filter-bulan').val(),
            bulan_awal: $('#periode-bulan-awal').val(),
            bulan_akhir: $('#periode-bulan-akhir').val()
          };

          if (params.bulan_awal && params.bulan_akhir && params.tahun) {
            params.bulan_awal = `${params.tahun}-${params.bulan_awal}`;
            params.bulan_akhir = `${params.tahun}-${params.bulan_akhir}`;
          }

          // Hapus params yang kosong
          Object.keys(params).forEach(key => {
            if (!params[key]) {
              delete params[key];
            }
          });

          $.ajax({
            url: '/api/data/sum',
            type: 'GET',
            data: params,
            success: function(response) {
              $('#sum-urea-text').text(response.urea?.toLocaleString('id-ID') || '0');
              $('#sum-npk-text').text(response.npk?.toLocaleString('id-ID') || '0');
              $('#sum-sp36-text').text(response.sp36?.toLocaleString('id-ID') || '0');
              $('#sum-za-text').text(response.za?.toLocaleString('id-ID') || '0');
              $('#sum-npk-formula-text').text(response.npk_formula?.toLocaleString('id-ID') || '0');
              $('#sum-organik-text').text(response.organik?.toLocaleString('id-ID') || '0');
              $('#sum-organik-cair-text').text(response.organik_cair?.toLocaleString('id-ID') || '0');
              $('#sum-kakao-text').text(response.kakao?.toLocaleString('id-ID') || '0');
              
              // Update info filter yang aktif
              $('#sum-kabupaten').text(params.kabupaten || 'Semua Kabupaten');
              $('#sum-kecamatan').text(params.kecamatan || 'Semua Kecamatan');
              $('#sum-metode').text(params.metode_penebusan || 'Semua Metode');
              
              if (params.bulan_awal && params.bulan_akhir) {
                $('#sum-tanggal').text(`Periode ${params.bulan_awal} s/d ${params.bulan_akhir}`);
              } else if (params.bulan && params.tahun) {
                const bulanNames = ['Januari', 'Februari', 'Maret', 'April', 'Mei', 'Juni', 'Juli', 'Agustus', 'September', 'Oktober', 'November', 'Desember'];
                $('#sum-tanggal').text(`${bulanNames[parseInt(params.bulan)-1]} ${params.tahun}`);
              } else if (params.tahun) {
                $('#sum-tanggal').text(`Tahun ${params.tahun}`);
              } else {
                $('#sum-tanggal').text('Semua Tanggal');
              }
            },
            error: function() {
              alert('Gagal memuat total data');
            },
            complete: function() {
              hideProcessing();
            }
          });
        }

        // Event handler untuk filter
        function setupFilterEvents() {
          // Reset filter tanggal saat memilih opsi yang berbeda
          $('#filter-tahun').on('change', function() {
            $('#filter-bulan').val('');
            $('#periode-bulan-awal').val('');
            $('#periode-bulan-akhir').val('');
          });

          $('#filter-bulan').on('change', function() {
            $('#periode-bulan-awal').val('');
            $('#periode-bulan-akhir').val('');
          });

          $('#periode-bulan-awal, #periode-bulan-akhir').on('change', function() {
            $('#filter-bulan').val('');
          });
          
          // Tombol Muat Data
          $('#load-data-btn').on('click', function() {
            loadData();
          });
          
          // Tombol Reset Filter
          $('#reset-filters').on('click', function() {
            $('.filter-dropdown').val('');
            $('#filter-tahun').val('');
            $('#filter-bulan').val('');
            $('#periode-bulan-awal').val('');
            $('#periode-bulan-akhir').val('');
            $('#download-excel').prop('disabled', true);
            $('#data-info').text('Data belum dimuat');
            
            if (dataTable && hasData) {
              dataTable.clear().draw();
              hasData = false;
              
              // Reset summary
              $('#sum-urea-text').text('0');
              $('#sum-npk-text').text('0');
              $('#sum-sp36-text').text('0');
              $('#sum-za-text').text('0');
              $('#sum-npk-formula-text').text('0');
              $('#sum-organik-text').text('0');
              $('#sum-organik-cair-text').text('0');
              $('#sum-kakao-text').text('0');
              $('#sum-kabupaten').text('-');
              $('#sum-kecamatan').text('-');
              $('#sum-metode').text('-');
              $('#sum-tanggal').text('-');
            }
          });
        }

        // Load data filter options
        // function loadFilterOptions() {

        //   $.ajax({
        //     url: '/api/filters',
        //     type: 'GET',
        //     success: function(data) {
        //       // Isi dropdown kabupaten
        //       const kabupatenSelect = $('#filter-kabupaten');
        //       data.kabupaten.forEach(item => {
        //         kabupatenSelect.append(`<option value="${item}">${item}</option>`);
        //       });

        //       // Isi dropdown kecamatan
        //       const kecamatanSelect = $('#filter-kecamatan');
        //       data.kecamatan.forEach(item => {
        //         kecamatanSelect.append(`<option value="${item}">${item}</option>`);
        //       });

        //       // Isi dropdown metode penebusan
        //       const metodeSelect = $('#filter-metode-penebusan');
        //       data.metode_penebusan.forEach(item => {
        //         metodeSelect.append(`<option value="${item}">${item}</option>`);
        //       });

        //       // Inisialisasi DataTable setelah filter dimuat
        //       initializeDataTable();
        //       setupFilterEvents();
        //     },
        //     error: function() {
        //       alert('Gagal memuat opsi filter');
        //     },
        //     complete: function() {
        //       hideProcessing();
        //     }
        //   });
        // }

        // Download Excel
        $('#download-excel').on('click', function() {
          if (!hasData) {
            alert('Tidak ada data yang bisa didownload. Silakan muat data terlebih dahulu.');
            return;
          }
          
          showProcessing();
          
          const params = {
            kabupaten: $('#filter-kabupaten').val(),
            kecamatan: $('#filter-kecamatan').val(),
            metode_penebusan: $('#filter-metode-penebusan').val(),
            tahun: $('#filter-tahun').val(),
            bulan: $('#filter-bulan').val(),
            bulan_awal: $('#periode-bulan-awal').val(),
            bulan_akhir: $('#periode-bulan-akhir').val()
          };

          if (params.bulan_awal && params.bulan_akhir && params.tahun) {
            params.bulan_awal = `${params.tahun}-${params.bulan_awal}`;
            params.bulan_akhir = `${params.tahun}-${params.bulan_akhir}`;
          }

          // Buat URL download
          let url = '/api/data/download?' + $.param(params);
          
          // Buat iframe untuk trigger download
          const iframe = document.createElement('iframe');
          iframe.style.display = 'none';
          iframe.src = url;
          document.body.appendChild(iframe);
          
          // Sembunyikan loading setelah beberapa detik (asumsi download selesai)
          setTimeout(function() {
            hideProcessing();
            document.body.removeChild(iframe);
          }, 3000);
        });

        // Mulai dengan memuat opsi filter
        loadFilterOptions();
      });
    </script>
  </body>
</html>